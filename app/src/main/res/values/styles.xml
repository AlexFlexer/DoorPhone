<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#1E88E5</item>
        <item name="colorPrimaryDark">#303F9F</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@null</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StandardTextView" parent="Widget.AppCompat.TextView">
        <item name="fontFamily">@font/product_sans</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="StandardButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily">@font/product_sans</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="StylizedFrameLayout">
        <item name="background">@drawable/background_stroke</item>
        <item name="android:padding">5dp</item>
    </style>

    <style name="StylizedTextView" parent="StandardTextView">
        <item name="background">@color/white</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>